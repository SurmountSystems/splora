[workspace]

[package]
name = "splora-frontend-shared"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
publish = false
description = "Path builders and JSON parsers for the indexer. Not the explorer UI."
license = "MIT"

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
